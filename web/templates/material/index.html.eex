<table class="table">
  <tr>
    <th>Event</th>
    <th>Visible</th>
    <th>Name</th>
    <th>Type</th>
    <th>Contents</th>

    <th></th>
  </tr>
  <%= for material <- @materials do %>
    <tr>
      <td><%= material.event_id %></td>
      <td><%= material.visible %></td>
      <td><%= material.name %></td>
      <td><%= material.type %></td>
      <td><%= material.contents %></td>
  
      <td>
        <%= link "Show", to: material_path(@conn, :show, material), class: "btn btn-default btn-sm" %>
        <%= link "Edit", to: material_path(@conn, :edit, material), class: "btn btn-default btn-sm" %>
        <%= link "Delete", to: material_path(@conn, :delete, material), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-sm" %>
      </td>
    </tr>
  <% end %>
</table>

<div class="btn-toolbar">
  <%= link "New material", to: material_path(@conn, :new), class: "btn btn-success" %>
</div>
